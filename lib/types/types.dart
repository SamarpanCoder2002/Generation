enum ChatMessageType{
  text,
  image,
  video,
  audio,
  document,
  location
}

enum ConnectionType{
  available,
  request,
  send
}

enum ThemeModeTypes{
  systemMode,
  lightMode,
  darkMode
}

enum MessageHolderType{
  me,
  other
}