enum ChatMessageType{
  text,
  image,
  audio,
  video,
  document,
  location
}

enum ConnectionType{
  available,
  request,
  send
}

enum ThemeModeTypes{
  systemMode,
  lightMode,
  darkMode
}

enum MessageHolderType{
  me,
  other
}